# Spring Profile (dev or prod)
SPRING_PROFILES_ACTIVE=dev

# Real Estate Calculator - Database Configuration
# 로컬 개발: localhost 또는 host.docker.internal
# 프로덕션: 실제 DB 서버 IP/호스트
REALESTATE_DB_URL=jdbc:postgresql://host.docker.internal:5432/realestate
REALESTATE_DB_USERNAME=postgres
REALESTATE_DB_PASSWORD=postgres

# GitHub Container Registry (선택사항)
# GHCR_PAT=your_github_personal_access_token
