name: CI - Build and Test

on:
  push:
    branches:
      - dev
      - feature/**
  pull_request:
    branches:
      - main
      - dev

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Main Page 빌드 테스트
      - name: Build Main Page Image
        run: |
          docker build -t main-page:test ./main-page

      # Nginx 설정 문법 검증 (기본 설정 체크)
      - name: Validate Nginx Configuration Syntax
        run: |
          echo "Checking Nginx configuration files exist..."
          test -f nginx/nginx.conf || exit 1
          test -f nginx/conf.d/default.conf || exit 1
          echo "✅ Nginx configuration files found"

          # 파일 내용이 비어있지 않은지 확인
          test -s nginx/nginx.conf || exit 1
          test -s nginx/conf.d/default.conf || exit 1
          echo "✅ Nginx configuration files are not empty"

          # upstream 설정이 있는지 확인 (기본 검증)
          grep -q "upstream main-page" nginx/conf.d/default.conf || exit 1
          grep -q "upstream highschool" nginx/conf.d/default.conf || exit 1
          echo "✅ Nginx upstream configurations found"

      # Docker Compose 문법 검증
      - name: Validate Docker Compose
        run: |
          docker compose config > /dev/null

      - name: Build Summary
        run: |
          echo "✅ Build completed successfully"
          echo "📦 Main Page image built"
          echo "🔧 Nginx configuration validated"
          echo "📋 Docker Compose configuration validated"
